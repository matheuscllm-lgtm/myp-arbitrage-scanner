name: Scan delivery moved to chat
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - name: Local collection and chat delivery
        run: echo "Request a fresh local scan in the conversation. Follow DELIVERY_CHAT.md. This workflow does not collect or publish market results."
